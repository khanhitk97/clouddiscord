name: Build CloudDiscord IPA

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create App Entry Point if missing
        run: |
          # Tự động tạo file khởi chạy @main nếu trong repo chưa có
          if ! grep -rq "@main" .; then
            echo "Đang tạo CloudDiscordApp.swift..."
            cat << 'EOF' > CloudDiscordApp.swift
          import SwiftUI

          @main
          struct CloudDiscordApp: App {
              var body: some Scene {
                  WindowGroup {
                      GalleryView()
                  }
              }
          }
          EOF
          fi

      - name: Generate Xcode Project with XcodeGen
        run: |
          brew install xcodegen
          
          # Tạo file định nghĩa project
          cat << 'EOF' > project.yml
          name: CloudDiscord
          options:
            bundleIdPrefix: com.clouddiscord
            deploymentTarget:
              iOS: "17.0"
          targets:
            CloudDiscord:
              type: application
              platform: iOS
              sources:
                - path: .
                  includes:
                    - "*.swift"
              settings:
                base:
                  PRODUCT_BUNDLE_IDENTIFIER: com.clouddiscord.app
                  GENERATE_INFOPLIST_FILE: YES
                  CODE_SIGNING_ALLOWED: NO
                  CODE_SIGNING_REQUIRED: NO
                  CODE_SIGN_IDENTITY: ""
          EOF

          xcodegen generate

      - name: Build .app with xcodebuild
        run: |
          xcodebuild clean build \
            -project CloudDiscord.xcodeproj \
            -scheme CloudDiscord \
            -sdk iphoneos \
            -configuration Release \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            BUILD_DIR="${{ runner.temp }}/build"

      - name: Package .app into .ipa
        run: |
          cd ${{ runner.temp }}/build/Release-iphoneos
          mkdir -p Payload
          cp -r *.app Payload/
          zip -r "CloudDiscord.ipa" Payload
          mkdir -p ${{ github.workspace }}/output
          mv CloudDiscord.ipa ${{ github.workspace }}/output/

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CloudDiscord-IPA
          path: output/CloudDiscord.ipa
          retention-days: 14
